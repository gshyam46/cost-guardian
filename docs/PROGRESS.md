# Sillage: current progress

## Current continuation: lively interactive design, 2026-09-15

ADR-54 and the revised [design contract](DESIGN_REFRESH.md) follow the founder's correction: upright, natural typography and a warmer, more lively experience. The public investigation now has clickable call-flow nodes, curved wake paths driven by replay, pause/restart/scrubbing, duration/token/cost lenses and a separate sample duration-rule comparison. The source explorer changes the displayed connection path. Sample state remains isolated from registration, authentication and customer telemetry.

Hanken Grotesk replaces the serif/italic headline treatment throughout Sillage, with IBM Plex Mono used for compact measurements. Peach/cream surfaces, brick controls, softer edges and restrained hover responses carry through landing, demo, registration and workspace sign-in. Motion follows user interaction, with reduced-motion behavior and keyboard operation. Matching wake icons and the confirmed-save signup/waitlist contract remain intact.

The final full frontend suite passed 410 tests across 14 suites. Both final modes build from 78 source inputs and include 13 recorded artifacts, with two locally hosted fonts totaling 49,412 bytes. Final browser/preview evidence and exact artifact hashes are recorded in [VALIDATION.md](VALIDATION.md). Hosted deployment and authenticated account provisioning remain separate work.

## Current continuation: cream and brick product design, 2026-09-15

ADR-53 and [DESIGN_REFRESH.md](DESIGN_REFRESH.md) preceded code. The landing/demo now centres on a substantial interactive investigation, with a compact workflow and evidence explanation. The complete Sillage workspace and public access journey share cream paper, brick actions, brown ink, locally hosted Instrument Serif/Manrope and an original wake mark. Favicon, touch icon, manifest colors, charts, sidebar, forms, empty/error states and notifications follow the same system.

The public flow uses **Sign up / Sign in / Register**, with optional company/project details collapsed. A successful committed registration clears the form and shows the dedicated waitlist page; direct visits and reloads remain neutral. Failed writes preserve the retryable form. Existing consent, storage, retention and private workspace authentication contracts remain intact.

Both isolated builds passed with matching fonts/assets. Verification passed 396 frontend tests, 29 public server/build tests, 53 backend static tests (one platform skip), 69 workspace browser scenarios, ten actual public browser/Mongo phases and all eight native deployment phases. Small mobile and desktop screenshots were inspected. The native browser harness now retains journey outcomes and precise cleanup diagnostics after an earlier cleanup failure; the complete rerun passed. [VALIDATION.md](VALIDATION.md) records the evidence.

Local previews are running at `http://127.0.0.1:3004` (public site/signup, separate local database) and `http://127.0.0.1:8001/welcome` (existing workspace and preserved data). The local public build contains a synthetic privacy contact. Hosted Vercel, a dedicated hosted interest database, real privacy contact and actual deployed acceptance remain pending. Registration does not yet create authenticated accounts or managed workspaces.

## Current continuation: public Vercel frontend, 2026-09-14

ADR-52 and [PUBLIC_LAUNCH.md](PUBLIC_LAUNCH.md) preceded implementation. Public-mode landing/demo/signup/privacy routes and responsive access pages now work independently of workspace authentication. Sign-in/onboarding checks readiness with a deadline, shows coming-soon/currently-unavailable feedback and offers a workspace link only when ready. The independent registration function records consented, unverified early-access interest in a separate Mongo store; it never claims success before commit. Duplicate protection, transactional rate limits, expiry, private CSV export and exact-contact deletion are implemented.

The final clean build passed 380 frontend tests, 23 server tests and six build/config checks. The built UI passed nine actual HTTP/browser/Mongo acceptance phases in 7.660 seconds, including backend outage registration, persistence through a new client, failed-write retry, duplicate concurrency and rate rollback. Desktop/mobile views were inspected; test-owned data and processes were cleaned. CI now has a separate public-launch job. [VALIDATION.md](VALIDATION.md) contains the proof and reproducible scope.

**Deployment remains pending:** Vercel CLI reports logged out; no project or hosted registration database is linked. A real privacy contact and private environment configuration are also required before collection. This implements early-access registration, not public account creation, managed workspaces or verified email login. The existing local workspace/Founder previews were preserved. [WORKFLOW.md](WORKFLOW.md) explains how the monitored application uses its LLM while Sillage uses deterministic analysis.

## Previous continuation: OpenTelemetry and OpenInference, 2026-09-13

ADR-51 and [OPENTELEMETRY.md](OPENTELEMETRY.md) replace bespoke wrapping as the preferred new integration direction: the 0.2.0 Python wheel offers a selected upstream OpenInference instrumentor and a numeric `SillageSpanProcessor`. Existing OTel applications can attach the processor without replacing their provider, sampler or other exporters. Connections selects the standards path explicitly; unqualified CLI commands retain native behavior for existing users.

The processor is implemented and its **23 focused tests pass** against the isolated OTel 1.37.0 SDK. These cover actual providers, unchanged external exporter behavior, real trace/span/parent IDs, scalar agent ancestry, concurrent traces, bounded/expired context, strict token aliases, ignored non-LLM/image operations, unknown terminal evidence, privacy boundaries, replay and bounded shutdown. The complete package suite passed **67 tests**, and separate installed-wheel checks verified the actual collector/worker pipeline. [VALIDATION.md](VALIDATION.md) records the exact artifact, final SDK/product tests and deployed-evidence limits.

The bridge preserves available agent context instead of inventing agent names or workflow IDs. Missing late/remote/framework context uses the service label, and references to uncollected parents remain explicit. Upstream OpenInference LiteLLM currently omits terminal finish evidence for some successful calls; the outcome stays unknown while IDs, timings and tokens remain useful. Raw content and upstream estimated prices are excluded. General OTLP reception, full non-LLM/RAG storage, cross-process propagation and real customer activation remain open.

Current installed-wheel acceptance passed four local phases in 36.144 seconds: programmatic OTel, actual OpenAI SDK transport, original Founder LiteLLM fallback calls and a LangChain model callback. OpenAI 2.54.0 repeated the provider matrix successfully; the same 0.2.0 wheel also passed the 35-event native regression. The standards checks exposed missing capture: four early-closed streams and one cancellation per OpenAI version yielded no ended upstream span, so there is no Sillage event for them. Captured OpenAI Responses returns/completed streams/returned failure and successful Founder LiteLLM returns remained unknown; Founder exceptions were reported errors. Run details now visibly label unknown outcomes. These are recorded coverage limits, not complete streaming/reliability acceptance. Overall test/build totals are maintained in [VALIDATION.md](VALIDATION.md).

## Historical Python install-and-run continuation, ADR-50

Following the founder's simpler-onboarding request, [INSTRUMENTATION.md](INSTRUMENTATION.md) and ADR-50 were recorded before code. The `sillage-observe` wheel packages the client library and adds `sillage-run`, with startup instrumentation for the verified OpenAI/LiteLLM versions. The default Connections path is now create key → install/configure/run → verify a real call; a handshake is optional, source mode is explicit, and manual Python/Node helpers remain available. Local compatibility checking is separate from collector and worker evidence. Current acceptance is recorded in [VALIDATION.md](VALIDATION.md).

Original Founder Path is running on ports 3002/8002, preserving the unrelated application on 3000/8000. Its backend now starts through the installed launcher with an owner-issued Sillage ingestion key; no application source edits were needed. Its existing fallback code uses LiteLLM and optional Langfuse callbacks, while the Sillage preview uses direct capture. Real Founder analysis still requires its existing external sign-in and provider configuration. No real Founder model traffic was generated or claimed. Local installed-SDK/collector acceptance processed 35 synthetic events; the default onboarding passed 69 browser scenarios and the complete eight-phase native journey. The Sillage preview has been refreshed with this build. [VALIDATION.md](VALIDATION.md) records exact versions, hashes and evidence boundaries.

At the end of ADR-50, remaining integration gaps included broader SDK versions, Node automatic capture, live exporter diagnostics, reload/multiprocess handling, retrieval/tool/workflow spans, standards bridging and independently verified customer activation. The current ADR-51 continuation above addresses selected standards instrumentation and numeric projection; general OTLP reception and the wider private-beta/production gates remain open.

## Historical query feedback and visible product changes, 2026-09-13

The product is now **Sillage**, selected by the founder for the meaning of a wake: understanding what passed through by the evidence it leaves. Existing integration and deployment identifiers remain compatible. A public landing page, interactive sample workspace, redesigned access screen and responsive sidebar make the product visible before instrumentation. The earlier Connections slice explains credentials, provides downloadable helpers and introduces expandable steps and distinguishes a test from a real receipt and processing. Overview independently shows recent source calls even when processed accounting is missing or unavailable; Live adds searchable calls and model/token visibility. Known failed-call duration and zero-duration rendering are corrected.

The contract was recorded before implementation in [EXPERIENCE.md](EXPERIENCE.md). Current checks are maintained in [VALIDATION.md](VALIDATION.md). Public signup, managed project/source provisioning, full RAG tracing and real customer/deployed activation remain open; no UI label or sample claims to implement them. The earlier engineering history below remains relevant to those release gates.

The final experience build passed 1,259 backend tests (two skips), 291 frontend tests, 151 harness tests, 67 browser scenarios and the full eight-phase native journey. The local preview now serves that build at `http://127.0.0.1:8001/welcome`; its existing database, worker and identity fixture were retained. This is completed local experience verification, not closure of the broader release gates.

As of 2026-09-12, changes based on application baseline `a66caf7`. Review/planning, the R0-01 baseline and bounded R1 source/ledger/summary repairs are implemented. R2 adds named OIDC access, fixed binding, roles, transactional audit, scoped write-only credentials and Python/Node capture without Langfuse. R3 adds customer-configurable per-call monitoring rules, readable threshold evidence and durable Slack delivery with owner test/activation and recent history. R1-R3 as whole phases remain in progress.

## Readiness

**Engineering prototype; not ready for production customer use or public self-service.** A private beta is the next release objective after R0-R3 and the gates in [LAUNCH.md](LAUNCH.md).

The intended audience includes AI SaaS/RAG/agent teams without existing telemetry or Langfuse. An operator-configured OIDC deployment now accepts direct terminal-call JSON, with owner-managed keys and Python/JavaScript background exporters and call/stream helpers. Developers can add explicit completion capture without implementing a process queue or having a Langfuse account. Project provisioning, broader framework instrumentation, full RAG workflow capture, self-service membership and independently verified customer activation remain unfinished; the bounded Python startup path above is now implemented.

## Current implementation

The direct/OIDC path now has [deployment packaging and an operator guide](../deploy/guardian/README.md), following [DEPLOYMENT.md](DEPLOYMENT.md) and ADR-47 before code. One image definition contains the production UI/API; independent processes run ingestion and optional notification delivery. Explicit bootstrap initializes matching state/indexes before the first login, rejects incompatible databases and preserves history. File-backed secrets, bounded read-only readiness and known-route static serving make the existing customer path runnable without development servers. [VALIDATION.md](VALIDATION.md) records native/browser, build and container evidence separately. Operators still provide Mongo, OIDC and TLS; this does not close customer activation or the operational release gates.

Historical deployment evidence from 2026-09-12: the native acceptance journey passed eight phases covering signed browser login, key/test, synthetic intake, independent worker, measurements, committed resolution/replay, logout, restart and database transport outage/recovery. That backend snapshot passed 1,230 tests with one platform skip. Compose/static-build checks passed; these earlier results do not certify the current 0.2.0 standards path. Docker image execution, actual public TLS/IdP and customer activation remain open.

- Setup now offers OpenAI Responses/Chat Completions recipes for Python/Node, including streams. Python sync/async and Node async helpers preserve application results/errors, read only safe final usage and reuse the existing background queue. Usage conflicts, invalid counters and cancellation remain explicit unknowns; cost is not estimated. [PROVIDERS.md](PROVIDERS.md) and ADR-46 record the pre-code contract and rationale. Actual SDK/local HTTP/Mongo and browser results are maintained in [VALIDATION.md](VALIDATION.md).

- Slack notification setup supports named-owner tests, activation/deactivation and eligible retries. New incidents and redacted delivery jobs commit atomically; a separate leased worker records acceptance, retries, failures and unconfirmed outcomes. Setup shows recent history and heartbeat; incident detail shows its delivery record without hiding investigation if history fails. The operator holds one secret in environment configuration. [NOTIFICATIONS.md](NOTIFICATIONS.md).

- Named project owners can save cost/duration limits and reported-error controls in Setup. Strict known measurements trigger from the first evaluated call; equality and unknown measurements do not. Readable incident evidence shows the measurement, threshold and evaluated revision, with the existing run/resolve workflow. Transactional authority/audit, revision conflicts and first-evaluation snapshots protect concurrent saves, lost acknowledgements and replay. [POLICIES.md](POLICIES.md).
- Separate Guardian API/worker/frontend and independent Founder demo.
- Shared observation normalization: stable source identity, UTC source time, revision fingerprint, explicit unknown cost/tokens/latency and safe issue codes.
- Default v2 REST traversal with explicit fields, opaque cursors, raw-byte/time/page budgets and safe diagnostics. Legacy v1 requires explicit selection and never acts as an automatic fallback.
- V2 detail maps distinguish measured zero from generated defaults; cached/reasoning buckets participate in normalization. HTTPX request logs omit cursor/host data.
- V2 worker commits up to five 100-row source pages per poll, including accepted/quarantined dispositions and durable continuation. Failed/malformed envelopes preserve progress; an out-of-horizon checkpoint blocks with an explicit backfill requirement. V1 keeps its complete-read-only, 500-row compatibility limit.
- Stable connection/source/project/trace/observation identity makes identical replay a no-op. Each new traversal rereads 24 hours; initial import accounts for every captured observation in that window. Changed copies become durable conflicts with unknown measurements.
- Mongo replica-set transactions and a connection lease fence protect ingestion, dirty work, bucket replacement and incident insertion. Numeric/identity snapshots exclude raw source content; PII work retains safe categories only.
- Detector jobs wait for window exhaustion before using earlier observations as a baseline. Rule failures remain pending; deterministic finding IDs preserve separate observations and human resolution on replay.
- Live summaries cover up to 1,000 accepted/read observations per bounded traversal; the displayed feed is limited separately. Partial, stale, unavailable and unknown measurement states are visible.
- Ledger-derived hourly rollups expose measurement/conflict coverage. Existing incremental rows remain identifiable as legacy and block an unsafe writer cutover; overlarge metric queries return a clear error.
- Authenticated worker monitoring separates ingestion health from API liveness. Blocking source execution is bounded outside the event loop.
- UI request cancellation, query-change guards and neutral API refresh labels prevent stale responses from appearing current. Empty run reads now report bounded absence rather than claiming a missing trace.
- Live/run views make no trace-list/get calls. Run queries cover at most seven days and distinguish bounded absence from source failure; workflow duration remains unknown.
- One incident summary response aggregates all matching records, bounds severity/detector categories, includes today in UTC and reports invalid-timestamp coverage. New writes add BSON dates; legacy aware dates convert without rewriting records. Failures/timeouts return unavailable rather than zero totals.
- Incident lists distinguish initial failure, successful empty results and failed refresh. Retry is visible; retained rows belong to the same filter and are labelled as the last successful response.
- A dedicated access endpoint is independent of monitoring. Local header/Bearer mode remains database-independent; OIDC checks the Mongo session, current configured membership and fixed deployment binding, without key fallback. All Guardian responses are noncacheable. [ACCESS.md](ACCESS.md), [IDENTITY.md](IDENTITY.md).
- Named OIDC code flow uses PKCE, nonce and browser-bound one-use state; verified issuer/subject maps to configured owner/operator/viewer roles. Opaque session hashes, absolute expiry, Secure/HttpOnly production cookies, exact Origin and CSRF protect browser access and writes.
- Fixed organization/project/environment/connection/issuer/client binding rejects deployment reassignment. Named resolution rechecks/touches authority transactionally and inserts one redacted actor audit; replay preserves the first resolution, and logout revokes the server session with majority acknowledgement.
- Browser access is verified before protected rendering. Local key storage/replacement recovery remains covered; OIDC ignores saved keys, keeps actor/project/CSRF in memory and reports logout failure with retry. Setup distinguishes configuration and observed work; direct mode adds owner key management and separate test/real receipts. Neither access success nor a test receipt claims activation.
- Direct numeric intake uses a strict versioned schema, server-fixed scope, hashed write-only keys and owner-only create/revoke with atomic audit. Operator/viewer roles see redacted metadata; full secrets are returned once and never recoverable from storage. [CAPTURE.md](CAPTURE.md).
- Credential authorization, admission quotas, idempotent batch receipts and numeric inbox records commit together. Exact replay adds no work; conflicting copies remain conservative conflicts. Test batches cannot affect production metrics or detectors. Concurrent initial source-mode claims serialize on the same database binding; changing an established mode requires an explicit migration decision.
- Direct worker transactions accept ledger records and acknowledge inbox work together, at most 500 records per cycle in batches of 100. Direct live/run views use bounded numeric ledger evidence with no Langfuse client, raw content or vendor links. Setup exposes receipt, pending and processed states independently.
- Dependency-free Python/Node exporters admit only copied numeric events to a bounded per-process queue. One sender preserves batch bytes/IDs through limited retries, isolates expired unsent events, stops on rejected credentials and exposes confirmed/unconfirmed/rejected counters. Prefix flush and bounded close handle concurrent producers and late receipts. Explicit call/stream helpers preserve provider results, exceptions and chunks, including Python async operations. No price, workflow success or provider-internal retry is inferred. [EXPORTING.md](EXPORTING.md).

All seven original review assertions now pass, including material spending after a known zero-cost baseline. Original expected outcomes were preserved and the last expected-failure marker was removed. Unknown prices cannot supply a free baseline. See [POLICIES.md](POLICIES.md) and [VALIDATION.md](VALIDATION.md) for the latest contract and checks.

## What is still open

R3 still needs aggregate budgets/error rates, evaluated noise and recall, deployed Slack/customer acceptance, external worker monitoring, richer incident lifecycle and measured recovery. Durable delivery and recent history are implemented locally. R1-02 still needs automated cutover/backup/rollback, source-delay/retention and operating evidence. A rebuild over 10,000 observations per bucket or a relative baseline over 5,000 rows remains pending with degraded health; immediate absolute/error rules can proceed independently. Backlog drain and starvation limits require load evidence. R1-03 still needs production query/index and legacy-conversion validation plus scalable metrics/list pagination.

The default Guardian reader now uses supported Observations v2. Explicit legacy mode remains for self-hosted v3/temporary rollback; its Cloud interfaces retire on 2026-11-16. [Official migration guide](https://langfuse.com/faq/all/deprecated-api-migration). Local wire tests do not prove a real server/exporter pairing. Founder still uses SDK2, and older exporter data can be delayed; migration and real source evidence remain pre-beta requirements.

Named access and scoped direct-ingestion credentials have local protocol/API/database evidence. Actual identity-provider registration, deployed TLS callback operation, membership invitations/removal/session administration, project creation and managed instrumentation remain open. Configured membership changes require consistent restarts across replicas. Direct capture excludes raw content; Langfuse source output/error previews still process it, so safe defaults remain unfinished for that mode. Deployed notification acceptance, the broader incident lifecycle and retention/offboarding are also unfinished. Direct receipts, inbox history, keys and audits remain retained; admission bounds are not capacity promises.

## Verification and evidence

The latest provider slice passes **260 frontend tests**, **142 standard-library tests**, **67 browser scenarios**, the production build and **four actual SDK/localhost API/Mongo pipelines**. Actual AsyncOpenAI adds four MockTransport call/stream paths and six SDK extra-field checks. The standard-library run includes 25 Python adapter tests and a Node runner with 44 assertions. Earlier 1,135-backend-test and 33-case ledger/capture/notification Mongo results remain evidence for unchanged backend code, not reruns in this client/frontend slice. Commands, exact reports and limitations are in [VALIDATION.md](VALIDATION.md); these local synthetic checks do not establish independent customer activation.

Earlier exporter verification includes independent Python/Node/backend admission parity and actual localhost HTTP/Mongo pipelines for both languages. A deliberately hidden committed receipt forces the identical batch through replay; those generic pipelines retain eight production calls, the known cost/token subtotal, explicit unknown measurements and two incidents. The newer provider pipelines each retain six production calls, known token subtotals, unknown costs and one error incident, with a separate test event. Deterministic lifecycle tests cover bounded queues, retry floors, expiry, cancellation and shutdown. Windows PID simulation covers inherited Python instance refusal; an actual POSIX fork was not exercised locally.

Backend, frontend/build, synthetic HTTP and browser results are recorded in [VALIDATION.md](VALIDATION.md); use that dated record for the latest completed checks. Identity evidence includes signed-token fixtures, controlled provider HTTP, independent API/store acceptance and real local Mongo state/race/rollback checks. Direct capture adds actual Mongo key/admission/ack rollback, quota and revocation races, exclusive initial capture-mode claims and real loopback HTTP intake through worker totals and an incident. Test traffic stays separate, and the direct path constructs no Langfuse client. Browser scenarios use synthetic API responses and do not establish actual IdP registration, live customer onboarding or deployed routing. Mock tests do not establish atomicity, and a single-node Mongo test does not establish production failover. No customer/provider/Langfuse service traffic was used for these local checks.

R0-01 previously established independent clean Python installations, per-app constraints and npm lockfiles, both frontend baselines, isolated harness/process tests and offline/manual-live CI workflows. Historical results remain in VALIDATION; hosted CI, live integration, credential closure and discovery/provisioning still need evidence.

## Implementation order

Next product work is a runnable deployment/onboarding path connecting the implemented OpenAI usage recipes to the incident/notification loop. Pricing provenance, additional providers, workflow/RAG outcomes and measured recovery remain explicit gaps; generic exporter expansion is not the priority. In parallel, finish cutover/recovery, query/load validation, the advertised source matrix, retention/offboarding and actual deployed identity/application/delivery checks. [PLAN.md](PLAN.md) owns the sequence; [POLICIES.md](POLICIES.md) and [NOTIFICATIONS.md](NOTIFICATIONS.md) record the current customer workflow.

The contract was documented before code; source, worker/API, live/UI and independent regression review proceeded in parallel. Decisions, architecture, plan, status and verification now track the resulting implementation. Canonical phase/ticket status remains [PHASES.md](PHASES.md); no phase is marked complete merely because local tests pass.
