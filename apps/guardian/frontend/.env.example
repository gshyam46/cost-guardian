# Port for the Guardian dashboard (3001 so it can run beside a monitored app on 3000)
PORT=3001
# Development API origin. Production should use the same HTTPS origin as the UI;
# set REACT_APP_GUARDIAN_URL=/ when building to override this local setting.
REACT_APP_GUARDIAN_URL=http://localhost:8001
