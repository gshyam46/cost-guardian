# FounderPath uses port 3000; the separate Guardian frontend uses port 3001.
PORT=3000
# Public API base URL. Provider credentials belong only in the backend environment.
REACT_APP_BACKEND_URL=http://localhost:8000
